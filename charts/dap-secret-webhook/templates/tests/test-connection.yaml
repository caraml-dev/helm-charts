apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dap-secret-webhook.fullname" . }}-test-connection"
  labels:
    {{- include "dap-secret-webhook.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dap-secret-webhook.fullname" . }}:{{ .Values.deployment.service.port }}']
  restartPolicy: Never
