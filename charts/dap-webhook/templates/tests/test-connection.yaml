apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dap-webhook.fullname" . }}-test-connection"
  labels:
    {{- include "dap-webhook.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dap-webhook.fullname" . }}:{{ .Values.deployment.service.port }}']
  restartPolicy: Never
