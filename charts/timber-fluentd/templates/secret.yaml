{{- if .Values.gcpServiceAccount.enabled -}}
apiVersion: v1
kind: Secret
metadata:
  namespace: {{ .Release.Namespace }}
  name: {{ .Release.Name }}-fluentd-secret
  labels:
    {{- include "timber-fluentd.labels" . | nindent 4 }}
data:
  service-account.json: |-
{{ .Values.gcpServiceAccount.account | indent 4 }}
{{- end }}
