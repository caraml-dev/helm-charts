apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "caraml.fullname" . }}-test-connection"
  labels:
    {{- include "caraml.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "caraml.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
