apiVersion: v1
kind: Service
metadata:
  name: {{ include "caraml.fullname" . }}
  labels:
    {{- include "caraml.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "caraml.selectorLabels" . | nindent 4 }}
