apiVersion: v1
kind: Service
metadata:
  name: {{ template "keto.fullname" . }}
  labels:
    app: {{ template "keto.name" . }}
    chart: {{ template "keto.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "keto.name" . }}
    release: {{ .Release.Name }}
